<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white flex items-center justify-center min-h-screen p-5">

    <div class="w-full max-w-xl">
        <!-- Logout Button -->
        <div class="w-full flex justify-end mb-5">
            <a class="bg-red-500 hover:bg-red-600 transition px-4 py-2 rounded-md text-sm" href="/logout">Logout</a>
        </div>

        <!-- Page Title -->
        <h3 class="text-2xl font-semibold mb-4 text-center">Edit Your Post</h3>

        <!-- Edit Post Form -->
        <form action="/update/<%= post._id %>" method="post" class="flex flex-col gap-3">
            <textarea 
                placeholder="What's on your mind?" 
                class="w-full min-h-[120px] p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md"
                name="content"><%= post.content %></textarea>
            
            <input class="px-4 py-2 bg-yellow-500 hover:bg-yellow-600 text-black font-semibold rounded-md cursor-pointer transition" type="submit" value="Update Post">
        </form>
    </div>

</body>
</html>
