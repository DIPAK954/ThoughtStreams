<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white flex items-center justify-center min-h-screen p-5">

    <div class="w-full max-w-md bg-zinc-800 p-8 rounded-lg shadow-md">
        <h3 class="text-2xl mb-5 text-center font-semibold">Upload Profile Picture</h3>

        <form action="/upload" autocomplete="off" method="post" enctype="multipart/form-data" class="flex flex-col gap-4">
            <label class="block">
                <span class="text-sm text-zinc-400">Choose an image:</span>
                <input type="file" name="image" class="block w-full text-sm text-zinc-300 file:mr-4 file:py-2 file:px-4 
                    file:rounded-md file:border-0 file:text-sm file:font-semibold 
                    file:bg-blue-500 file:text-white hover:file:bg-blue-600 transition">
            </label>

            <input class="px-5 py-2 bg-blue-500 hover:bg-blue-600 transition text-white rounded-md cursor-pointer text-center" type="submit" value="Upload File">
        </form>
    </div>

</body>
</html>
